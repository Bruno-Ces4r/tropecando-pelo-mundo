// Applies to every page under src/pt/.
export default {
  lang: "pt",
  altLang: "en",
  layout: "base.njk",
  // altPath is the path (after the language prefix) to the equivalent page in
  // the other language. Pages override this in front matter; home defaults to "/".
  altPath: "/",
};
